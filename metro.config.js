//////////////////////////////////////////////////////////////////////
// Customized metro config
// SPDX-License-Identifier: GPL-3.0
const { getDefaultConfig } = require('expo/metro-config');
const defaultConfig = getDefaultConfig(__dirname);

module.exports = Object.assign(defaultConfig, {
  
});
